/** Fachada da feature `ranking-clientes` (server-only — arrasta Prisma). */
export type { RankingClientesRow } from './ranking-clientes.contract';
export { RankingClientesService, rankingClientesService } from './services/ranking-clientes.service';
