# Story 2.3: Vendas por Item aberto por Loja

## Status

Done

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@architect"
```

## Story

**As a** gestor,
**I want** ver a mesma visão de vendas por item, segmentada por loja,
**so that** eu compare o desempenho de produtos entre lojas (Requisito 6).

## Acceptance Criteria

1. Mesma visão da Story 2.2, com dimensão adicional de Loja.
2. Permite comparar quantidade vendida do mesmo item entre lojas.

## Tasks / Subtasks

- [x] Task 1: Query de agregação com Loja (AC: 1, 2)
  - [x] `groupBy({ by: ['loja', 'familia', 'grupo', 'item'], _sum: { quantidade: true } })` — mesma base da 2.2, `loja` adicionada ao agrupamento (`findAgrupadoPorItemELoja`, no mesmo repositório da 2.2)
- [x] Task 2: UI (AC: 1, 2)
  - [x] Reaproveitar o componente de tabela da 2.2 (REUSE, não recriar) com a coluna/dimensão Loja adicionada — `VendasPorItemTable` detecta a presença de `loja` na linha e mostra a coluna condicionalmente
- [x] Task 3: Testes e validação
  - [x] Testes unitários cobrindo o agrupamento com a dimensão Loja — incluído no `vendas-por-item.service.test.ts` da 2.2
  - [x] Confirmar `npm run lint`, `npm run typecheck`, `npm test` — ✅ lint, ✅ typecheck, ✅ 133/133, ✅ build
  - [x] Atualizar File List

## Dev Notes

**Previous Story Insights**: Depende diretamente da 2.2 — mesma feature, dimensão extra. Não duplicar o componente de tabela; parametrizar pelas colunas de agrupamento.

**File Locations**: `packages/web/src/features/reports/vendas-por-item/` (mesma feature da 2.2, não uma nova).

**Dependencies**: Depende da Story 2.2.

### Testing

- **Cenários mínimos**: mesmo item em lojas diferentes aparece como linhas separadas; total por item ainda é consistente somando as lojas.

## Change Log

| Date | Version | Description | Author |
|------|---------|--------------|--------|
| 2026-08-31 | 0.1 | Story criada a partir da Epic 2 do PRD, em lote com as demais stories de relatório | @sm (via sessão combinada) |
| 2026-08-31 | 0.2 | Auto-validada GO — Status: Draft → Ready | @po (via sessão combinada) |
| 2026-08-31 | 0.3.0 | Development started/complete (sessão contínua, em lote com 2.1/2.2/2.5/3.1–3.3) — Status: Ready → InProgress → InReview. AC1/AC2 implementadas, reaproveitando a feature/tabela da 2.2 (sem duplicação). Validações: lint ✅, typecheck ✅, 133 testes ✅, build ✅ | @dev |
| 2026-09-01 | 0.4.0 | QA Gate CONCERNS — Status: InReview → Done. Reuso confirmado como genuíno. Achado: TEST-001 (medium — limitação de ambiente recorrente). Gate: `docs/qa/gates/2.3-vendas-por-item-aberto-por-loja.yml` | @qa |
| 2026-09-03 | 0.5.0 | **Amendment pós-Done, sem reabrir a story.** Herda a coluna `Faturamento` adicionada na Story 2.2 (feature compartilhada, mesmo `VendasPorItemTable`) — sem trabalho extra nesta story, o reuso já cobria isso automaticamente | @dev |
| 2026-09-03 | 0.6.0 | **Amendment pós-Done, sem reabrir a story.** Herda `Linha`/`Marca`/`Tipo de Preço` adicionados na Story 2.2 — mesmo motivo, mesmo reuso | @dev |

## Dev Agent Record

### Agent Model Used

claude-sonnet-5 (@dev / Dex) — modo interactive, implementada em lote junto com a Story 2.2 (mesma feature).

### Debug Log References

Validações: `npm run lint`, `npm run typecheck`, `npm test`, `npm run build`.

### Completion Notes List

**Entregue: AC1, AC2.**

Sem feature nova: `VendasPorItemRepository`/`Service`/`VendasPorItemTable` da Story 2.2 já
foram desenhados com o método/variante "por loja" desde o início (`findAgrupadoPorItemELoja`,
`getPorItemPorLoja`), exatamente para esta story não duplicar nada. `VendasPorItemTable`
detecta em runtime se a linha tem a chave `loja` e mostra a coluna condicionalmente — mesmo
componente, duas rotas (`/relatorios/vendas-por-item` e
`/relatorios/vendas-por-item-por-loja`).

### File List

**Criados**

- `packages/web/src/app/(protected)/relatorios/vendas-por-item-por-loja/page.tsx`

**Nenhum arquivo de `features/reports/vendas-por-item/` foi criado por esta story** — todos já existiam da 2.2 (ver File List da 2.2).

## QA Results

### Review Date: 2026-09-01

### Reviewed By: Quinn (Test Architect)

### Reviewed Revision: commit:5b2630a

Revisão em conjunto com as demais 6 stories desta leva. Confirmei que o reuso é genuíno:
nenhum arquivo novo em `features/reports/vendas-por-item/`, só a rota consumindo o método
já existente da 2.2.

### Compliance Check

- Testing Strategy: ✓ com ressalva — TEST-001 (limitação de ambiente recorrente)
- All ACs Met: ✓ — AC1/AC2

### Gate Status

Gate: CONCERNS → docs/qa/gates/2.3-vendas-por-item-aberto-por-loja.yml

### Lifecycle Transition

CONCERNS: InReview → Done (aplicada no Status e no Change Log desta story).
