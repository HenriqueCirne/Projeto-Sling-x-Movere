# Story 2.3: Vendas por Item aberto por Loja

## Status

Ready

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@architect"
```

## Story

**As a** gestor,
**I want** ver a mesma visão de vendas por item, segmentada por loja,
**so that** eu compare o desempenho de produtos entre lojas (Requisito 6).

## Acceptance Criteria

1. Mesma visão da Story 2.2, com dimensão adicional de Loja.
2. Permite comparar quantidade vendida do mesmo item entre lojas.

## Tasks / Subtasks

- [ ] Task 1: Query de agregação com Loja (AC: 1, 2)
  - [ ] `groupBy({ by: ['loja', 'familia', 'grupo', 'item'], _sum: { quantidade: true } })` — mesma base da 2.2, `loja` adicionada ao agrupamento
- [ ] Task 2: UI (AC: 1, 2)
  - [ ] Reaproveitar o componente de tabela da 2.2 (REUSE, não recriar) com a coluna/dimensão Loja adicionada
- [ ] Task 3: Testes e validação
  - [ ] Testes unitários cobrindo o agrupamento com a dimensão Loja
  - [ ] Confirmar `npm run lint`, `npm run typecheck`, `npm test`
  - [ ] Atualizar File List

## Dev Notes

**Previous Story Insights**: Depende diretamente da 2.2 — mesma feature, dimensão extra. Não duplicar o componente de tabela; parametrizar pelas colunas de agrupamento.

**File Locations**: `packages/web/src/features/reports/vendas-por-item/` (mesma feature da 2.2, não uma nova).

**Dependencies**: Depende da Story 2.2.

### Testing

- **Cenários mínimos**: mesmo item em lojas diferentes aparece como linhas separadas; total por item ainda é consistente somando as lojas.

## Change Log

| Date | Version | Description | Author |
|------|---------|--------------|--------|
| 2026-08-31 | 0.1 | Story criada a partir da Epic 2 do PRD, em lote com as demais stories de relatório | @sm (via sessão combinada) |
| 2026-08-31 | 0.2 | Auto-validada GO — Status: Draft → Ready | @po (via sessão combinada) |

## Dev Agent Record

### Agent Model Used

_A ser preenchido pelo @dev durante a implementação._

### Debug Log References

_A ser preenchido pelo @dev durante a implementação._

### Completion Notes List

_A ser preenchido pelo @dev durante a implementação._

### File List

_A ser preenchido pelo @dev durante a implementação._

## QA Results

_A ser preenchido pelo @qa durante o gate de qualidade._
