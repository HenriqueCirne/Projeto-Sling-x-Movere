# Story 2.2: Vendas por Item (Família/Grupo/Marca/Linha)

## Status

InReview

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@architect"
```

## Story

**As a** gestor,
**I want** ver a quantidade vendida agrupada por Família, Grupo e Item,
**so that** eu identifique quais produtos vendem mais (Requisito 2).

## Acceptance Criteria

1. Tabela exibe quantidade total vendida agrupada por Família, Grupo e Item.
2. Filtro por período.
3. Ordenação por quantidade.

## Tasks / Subtasks

- [x] Task 1: Query de agregação (AC: 1, 3)
  - [x] `prisma.salesEntry.groupBy({ by: ['familia', 'grupo', 'item'], _sum: { quantidade: true }, orderBy: { _sum: { quantidade: 'desc' } } })` filtrado por período — type-safe, sem SQL cru (mesmo padrão da 1.5/2.1)
- [x] Task 2: UI de tabela (AC: 1, 3)
  - [x] Tabela com colunas Família/Grupo/Item/Quantidade, já ordenada pela query — `VendasPorItemTable`
- [x] Task 3: Filtro de período (AC: 2)
  - [x] Reutilizar o componente de filtro compartilhado (Story 1.5/2.1) — `ReportFilterForm` sem `showLoja` (AC desta story não pede filtro por loja)
- [x] Task 4: Testes e validação
  - [x] Testes unitários da agregação/ordenação com repositório fake — 4 testes
  - [x] Confirmar `npm run lint`, `npm run typecheck`, `npm test` — ✅ lint, ✅ typecheck, ✅ 133/133, ✅ build
  - [x] Atualizar File List

## Dev Notes

**File Locations**: `packages/web/src/features/reports/vendas-por-item/`.

**Technical Constraints**: `familia`/`grupo`/`item` são nullable no schema da Story 1.3 (cobertura de campo da API não validada, TD-03) — linhas com esses campos ausentes devem aparecer agrupadas sob um rótulo explícito (ex: "Não informado"), nunca silenciosamente descartadas do agrupamento.

**Dependencies**: Depende da Story 1.3. Compartilha o filtro de período com 2.1/1.5.

### Testing

- **Cenários mínimos**: agrupamento correto por Família/Grupo/Item, ordenação decrescente por quantidade, linhas com campo nulo aparecem sob "Não informado" em vez de desaparecerem.

## Change Log

| Date | Version | Description | Author |
|------|---------|--------------|--------|
| 2026-08-31 | 0.1 | Story criada a partir da Epic 2 do PRD, em lote com as demais stories de relatório | @sm (via sessão combinada) |
| 2026-08-31 | 0.2 | Auto-validada GO — Status: Draft → Ready | @po (via sessão combinada) |
| 2026-08-31 | 0.3.0 | Development started/complete (sessão contínua, em lote com 2.1/2.3/2.5/3.1–3.3) — Status: Ready → InProgress → InReview. AC1–AC3 implementadas. Validações: lint ✅, typecheck ✅, 133 testes ✅, build ✅ | @dev |

## Dev Agent Record

### Agent Model Used

claude-sonnet-5 (@dev / Dex) — modo interactive, implementada em lote com as demais stories de relatório da Epic 2/3 (ver Story 2.1 para o contexto do refactor compartilhado de filtro/where).

### Debug Log References

Validações: `npm run lint`, `npm run typecheck`, `npm test`, `npm run build`, validação manual via dev server (rota redireciona corretamente sem sessão).

### Completion Notes List

**Entregue: AC1–AC3.**

Feature `features/reports/vendas-por-item/`: `groupBy(['familia','grupo','item'])` com `_sum.quantidade` e `orderBy` já no banco (AC3, sem reordenar em memória). Campos nulos (TD-03: cobertura de campos da API não validada) viram `"Não informado"` (`labelOrNaoInformado`, `shared/reports/labels.ts`) em vez de a linha desaparecer do agrupamento — uma linha descartada silenciosamente mudaria os totais sem aviso.

A feature já nasce compartilhada com a Story 2.3 (`VendasPorItemRepository`/`Service`/`Table` têm os métodos/variante "por loja" desde o início), evitando duplicar a tabela depois.

**Resultados das validações:**

| Comando | Resultado |
|---------|-----------|
| `npm run lint` | ✅ sem erros |
| `npm run typecheck` | ✅ sem erros |
| `npm test` | ✅ 133 testes / 23 arquivos |
| `npm run build` | ✅ build de produção OK |

### File List

**Criados**

- `packages/web/src/features/reports/vendas-por-item/vendas-por-item.contract.ts`
- `packages/web/src/features/reports/vendas-por-item/index.ts`
- `packages/web/src/features/reports/vendas-por-item/repositories/vendas-por-item.repository.ts`
- `packages/web/src/features/reports/vendas-por-item/services/vendas-por-item.service.ts` + `.test.ts`
- `packages/web/src/features/reports/vendas-por-item/components/VendasPorItemTable.tsx`
- `packages/web/src/app/(protected)/relatorios/vendas-por-item/page.tsx`
- `packages/web/src/shared/reports/labels.ts` + `.test.ts`

## QA Results

_A ser preenchido pelo @qa durante o gate de qualidade._
