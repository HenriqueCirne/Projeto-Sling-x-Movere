# Story 1.4: Job de sincronização diária com a API Moveres Software

## Status

Ready

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools:
  - "npm run lint"
  - "npm run typecheck"
  - "npm test"
  - "coderabbit --prompt-only -t uncommitted"
```

> **[AUTO-DECISION]** Campo "Executor Assignment" não estava presente no epic → River (SM) atribuiu `executor: @dev` / `quality_gate: @architect` (reason: implementação do cliente HTTP e do job é "Code/Features/Logic"; @architect como quality gate porque a story introduz um novo padrão de integração externa com riscos técnicos não validados).

## Story

**As a** gestor,
**I want** que os dados de vendas sincronizem automaticamente uma vez por dia a partir do ERP,
**so that** eu não dependa de atualização manual.

## Acceptance Criteria

1. Job agendado autentica na API Moveres Software usando as credenciais de `.env` (`MOVERE_API_*`).
2. Job busca lançamentos novos/atualizados e grava/atualiza na tabela `sales_entries`.
3. Execuções do job são registradas em log (sucesso/erro), atendendo NFR6.
4. Job pode ser executado manualmente para fins de teste/backfill inicial.
5. Falha na sincronização não derruba a aplicação (tratamento de erro isolado).

## 🤖 CodeRabbit Integration

### Story Type Analysis

**Primary Type**: Integration
**Secondary Type(s)**: Deployment/Infrastructure (job agendado), Database (upsert em `sales_entries`)
**Complexity**: High — depende de uma API externa cuja cobertura de campos, paginação e rate limits ainda não foram validados tecnicamente; exige lógica de agendamento, tratamento de erro isolado e logging estruturado.

### Specialized Agent Assignment

**Primary Agents**:
- @dev (cliente HTTP, lógica de sincronização, tratamento de erro)
- @architect (padrão de integração e estratégia de agendamento)

**Supporting Agents**:
- @devops (infraestrutura do job agendado/cron — operação de CI/CD é exclusiva de @devops)
- @data-engineer (revisão do upsert em `sales_entries`)

### Quality Gate Tasks

- [ ] Pre-Commit (@dev): rodar `npm run lint`, `npm run typecheck`, `npm test` e `coderabbit --prompt-only -t uncommitted` antes de marcar a story como completa
- [ ] Pre-PR (@devops): rodar `coderabbit --prompt-only --base main` antes de abrir PR
- [ ] Pre-Deployment (@devops): validar configuração do agendamento e ausência de segredos hardcoded antes de habilitar em produção

### Self-Healing Configuration

**Expected Self-Healing**:
- Primary Agent: @dev (light mode)
- Max Iterations: 2
- Timeout: 15 minutes
- Severity Filter: CRITICAL

**Predicted Behavior**:
- CRITICAL issues: auto_fix (até 2 iterações) — especialmente vazamento de credenciais em logs
- HIGH issues: document_only

### CodeRabbit Focus Areas

**Primary Focus**:
- Tratamento de erro e isolamento de falhas (AC5) — sincronização nunca deve derrubar a aplicação
- Idempotência do upsert em `sales_entries` (AC2)
- Nenhuma credencial (`MOVERE_API_*`) exposta em logs, erros ou commits (NFR4)

**Secondary Focus**:
- Logging estruturado de cada execução (AC3, NFR6)
- Resiliência a rate limits/timeouts desconhecidos da API Moveres

## Tasks / Subtasks

- [ ] Task 1: Cliente HTTP para a API Moveres Software (AC: 1)
  - [ ] Implementar autenticação na API usando as variáveis de ambiente `MOVERE_API_BASE_URL`, `MOVERE_API_ENVIRONMENT`, `MOVERE_API_USER`, `MOVERE_API_PASSWORD` (nomes definidos em `.env`/`.env.example` — **nunca usar ou expor os valores reais em código, commits ou logs**) [Source: .env.example]
  - [ ] Validar o payload recebido com Zod antes de persistir, conforme decisão já registrada no PRD [Source: docs/prd/technical-assumptions.md#Additional Technical Assumptions and Requests]
  - [ ] **RISCO CONHECIDO:** a cobertura exata de campos da API Moveres Software (endpoints, paginação, rate limits) ainda não foi validada tecnicamente [Source: docs/brief.md#Key Risks; docs/brief.md#Areas Needing Further Research]. Consultar o Swagger da API (`https://api.moveresoftware.com/swagger/ui/index#/`) como primeira subtask, antes de implementar o cliente
- [ ] Task 2: Lógica de busca incremental e upsert (AC: 2)
  - [ ] Implementar busca de lançamentos novos/atualizados (verificar se a API Moveres suporta filtro incremental por data; caso não suporte, buscar todo o período configurado e fazer upsert)
  - [ ] Implementar upsert idempotente na tabela `sales_entries` (Story 1.3), usando o identificador único do lançamento vindo do ERP
- [ ] Task 3: Agendamento do job (AC: 1 — coordenar com @devops)
  - [ ] Configurar execução agendada diária (cron/scheduled job); mecanismo específico (ex: Vercel Cron, GitHub Actions scheduled workflow, ou outro) a confirmar com @devops/@architect, já que a hospedagem ainda não foi definida [Source: docs/brief.md#Technology Preferences]
- [ ] Task 4: Execução manual (AC: 4)
  - [ ] Expor comando/rota para disparar o job manualmente (uso em backfill inicial e testes), respeitando o princípio CLI First (Constitution Artigo I) [Source: .aiox-core/constitution.md]
- [ ] Task 5: Logging de execuções (AC: 3, NFR6)
  - [ ] Registrar em log cada execução do job: início, fim, quantidade de registros processados, sucesso/erro [Source: docs/prd/requirements.md#Non Functional]
- [ ] Task 6: Isolamento de falhas (AC: 5)
  - [ ] Garantir que exceções durante a sincronização são capturadas e não propagam para derrubar a aplicação principal (try/catch isolado no job, sem impactar requisições HTTP da aplicação)
- [ ] Task 7: Testes e validação
  - [ ] Testes de integração do job com mock da API Moveres [Source: docs/prd/technical-assumptions.md#Testing Requirements: Unit + Integration]
  - [ ] Testar cenário de falha da API (timeout, erro 4xx/5xx) e confirmar que o log registra o erro sem derrubar a aplicação
  - [ ] Confirmar que `npm run lint`, `npm run typecheck` e `npm test` passam
  - [ ] Atualizar File List

## Dev Notes

**Previous Story Insights**: Depende da Story 1.3 (tabela `sales_entries` deve existir com todos os campos necessários). Revisar o Dev Agent Record da 1.3 para confirmar nomes exatos de campos/tipos antes de mapear o payload da API.

**🛑 Spike técnico executado (2026-08-31), story ainda BLOQUEADA:** ver
`docs/architecture/api-moveres-contract-spike.md` e a atualização de TD-03 em
`docs/architecture/tech-decisions.md`. Resumo: autenticação (`POST
/api/LoginComAmbiente`), paginação (`pagina`) e filtro incremental
(`emissaoInicial`/`emissaoFinal`/`codigoLoja`, todos obrigatórios) confirmados contra a
API real. **Porém os itens de linha (`produtos`) e parcelas (`parcelas`) vêm sempre
vazios**, e o endpoint dedicado a itens de linha
(`/api/NotasFiscaisEmitidasPorEstruturaDeItens`) retorna zero resultados — a conta
configurada tem `grupo: "SEM ACESSO"`. **Não iniciar a Task 1 desta story até essa
permissão ser resolvida junto ao Moveres/administrador da conta** — sem itens de linha,
`item`/`familia`/`grupo`/`marca`/`linha` e quantidade/preço por item (FR3, FR4, FR8,
FR9) não são populáveis, e qualquer código escrito agora seria especulativo (Artigo IV
da Constitution — No Invention).

**Data Models**: Grava na tabela `sales_entries` definida na Story 1.3 — nenhum novo modelo de dados nesta story, apenas o mapeamento do payload da API para os campos existentes.

**API Specifications**: API REST da Moveres Software, base `https://api.moveresoftware.com`, Swagger disponível em `https://api.moveresoftware.com/swagger/ui/index#/`, documentação em `https://meajuda.moveresoftware.com/a/solutions/articles/27000069342`, autenticação via usuário/senha [Source: docs/brief.md#Technical Considerations; docs/brief.md#Appendices]. O ambiente é identificado pela variável `MOVERE_API_ENVIRONMENT` — assumido como identificador único de tenant ("CirnePneus"), sem múltiplos ambientes a gerenciar (premissa registrada no Project Brief, ainda não validada tecnicamente) [Source: docs/brief.md#Key Assumptions].

**Component Specifications**: N/A (job de backend, sem UI).

**File Locations**: `packages/web/src/features/erp-sync/` seguindo Contract/Service/Repository Pattern do preset [Source: .aiox-core/data/tech-presets/nextjs-react.md#Design Patterns (The Essential 5)].

**Testing Requirements**: Integração com mock da API Moveres, usando MSW (disponível no preset) [Source: .aiox-core/data/tech-presets/nextjs-react.md#Tech Stack; docs/prd/technical-assumptions.md#Testing Requirements: Unit + Integration].

**Technical Constraints**:
- NFR1: dados não podem ter mais de 24h de defasagem — reforça a necessidade de execução diária confiável [Source: docs/prd/requirements.md#Non Functional].
- NFR4: credenciais do ERP nunca em código-fonte versionado — usar exclusivamente as variáveis de ambiente já existentes em `.env` (`MOVERE_API_BASE_URL`, `MOVERE_API_ENVIRONMENT`, `MOVERE_API_USER`, `MOVERE_API_PASSWORD`); nunca imprimir valores reais em logs [Source: docs/prd/requirements.md#Non Functional; .env.example].
- NFR6: falhas devem ser logadas para diagnóstico técnico [Source: docs/prd/requirements.md#Non Functional].
- Mecanismo de agendamento (cron) e hospedagem ainda não definidos — decisão pendente de @architect/@devops [Source: docs/brief.md#Technology Preferences].
- Rate limits da API Moveres desconhecidos — não assumir limites específicos; implementar tratamento de erro genérico e resiliente.
- Nenhum documento de arquitetura (`docs/architecture/`) existe ainda neste projeto.

**Dependencies**: Depende da Story 1.3 (tabela `sales_entries`). A Story 1.5 (Painel) depende desta story — precisa de dados sincronizados para exibir KPIs reais.

### Testing

- **Test file location**: colocation `*.test.ts` dentro de `packages/web/src/features/erp-sync/`
- **Testing framework**: Vitest + MSW para mock da API Moveres
- **Cenários mínimos**: sincronização bem-sucedida (insert + update), falha de autenticação na API, timeout/erro 5xx da API (log registrado, aplicação não derruba), execução manual do job, idempotência (rodar o job duas vezes não duplica registros).

## Change Log

| Date | Version | Description | Author |
|------|---------|--------------|--------|
| 2026-08-27 | 0.1 | Story criada a partir da Epic 1 do PRD | River (SM) |
| 2026-08-27 | 0.2 | Validated GO (9/10) — Status: Draft → Ready | @po |
| 2026-08-27 | 0.2 | Should-fix registrado (não bloqueante): a Task 1 embute uma descoberta técnica de escopo aberto (validar Swagger da API Moveres — cobertura de campos, paginação, rate limits). Se a descoberta revelar divergência relevante em relação ao schema da Story 1.3, PARAR e acionar @po/@architect antes de adaptar o schema por conta própria | @po |
| 2026-08-27 | 0.2 | Condição de entrada registrada: mecanismo de agendamento e hospedagem indefinidos (AC1). AC2–AC5 são plenamente implementáveis sem essa decisão. Recomendação @po: implementar o gatilho manual (AC4) primeiro e tratar o agendamento (AC1) como último passo, delegado a @devops | @po |
| 2026-08-27 | 0.2 | **Referência técnica: `docs/architecture/tech-decisions.md` (TD-03, TD-04).** **⚠️ RISCO ABERTO (TD-03):** o @architect NÃO conseguiu validar o contrato da API Moveres — o Swagger em `api.moveresoftware.com/swagger/ui/index` serve apenas o shell JS (sem endpoints/schemas) e `/swagger/docs/v1` e `/swagger/v1/swagger.json` retornam **404**. Credenciais `MOVERE_API_*` deliberadamente NÃO utilizadas pelo @architect. Estabelecido pela doc pública: auth por **`Authorization: Bearer <token>`**, usuário precisa da rotina **[R1000 — Centro de Integração]**, environment fornecido pelo suporte; a API é segmentada em **grupos** (existem *API de Vendas*, *API Notas Fiscais Emitidas*, *API Notas Emitidas por Estrutura de Itens*); APIs riscadas no Swagger estão obsoletas. **Permanecem NÃO validados:** cobertura de campos, consulta incremental, paginação e rate limits. **Recomendação: executar um spike técnico com credenciais reais ANTES desta story**, entregando o mapa campo-a-campo API → `sales_entries`. **TD-04b — idempotência (AC2/cenário de teste "rodar duas vezes não duplica"):** não há chave natural de linha no export do ERP; verificar no spike se a API expõe ID de linha (preferencial); se não, usar **sincronização por janela (delete-and-replace por `Data de Emissão`+`Loja`)**, que é idempotente sem chave natural e seguro por a app ser somente-leitura. **NÃO** fazer upsert por `Id Lançamento` isolado. Mitigação estrutural: isolar o cliente HTTP atrás de um anti-corruption layer com contrato Zod próprio. Atenção: devoluções chegam com valor **negativo** — validação Zod não pode usar `.positive()` | Aria (@architect) |
| 2026-08-31 | 0.3 | Spike técnico da API Moveres executado (recomendação do TD-03) — ver `docs/architecture/api-moveres-contract-spike.md`. Autenticação/paginação/incremento confirmados; BLOQUEIO encontrado: itens de linha e parcelas retornam vazios (grupo de permissão "SEM ACESSO" na conta configurada). Story permanece **Ready**, não iniciada — ação humana necessária antes da Task 1 | @dev |

## Dev Agent Record

### Agent Model Used

_A ser preenchido pelo @dev durante a implementação._

### Debug Log References

_A ser preenchido pelo @dev durante a implementação._

### Completion Notes List

_A ser preenchido pelo @dev durante a implementação._

### File List

_A ser preenchido pelo @dev durante a implementação._

## QA Results

_A ser preenchido pelo @qa durante o gate de qualidade._
