# Story 1.4: Job de sincronização diária com a API Moveres Software

## Status

Done

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools:
  - "npm run lint"
  - "npm run typecheck"
  - "npm test"
  - "coderabbit --prompt-only -t uncommitted"
```

> **[AUTO-DECISION]** Campo "Executor Assignment" não estava presente no epic → River (SM) atribuiu `executor: @dev` / `quality_gate: @architect` (reason: implementação do cliente HTTP e do job é "Code/Features/Logic"; @architect como quality gate porque a story introduz um novo padrão de integração externa com riscos técnicos não validados).

## Story

**As a** gestor,
**I want** que os dados de vendas sincronizem automaticamente uma vez por dia a partir do ERP,
**so that** eu não dependa de atualização manual.

## Acceptance Criteria

1. Job agendado autentica na API Moveres Software usando as credenciais de `.env` (`MOVERE_API_*`).
2. Job busca lançamentos novos/atualizados e grava/atualiza na tabela `sales_entries`.
3. Execuções do job são registradas em log (sucesso/erro), atendendo NFR6.
4. Job pode ser executado manualmente para fins de teste/backfill inicial.
5. Falha na sincronização não derruba a aplicação (tratamento de erro isolado).

## 🤖 CodeRabbit Integration

### Story Type Analysis

**Primary Type**: Integration
**Secondary Type(s)**: Deployment/Infrastructure (job agendado), Database (upsert em `sales_entries`)
**Complexity**: High — depende de uma API externa cuja cobertura de campos, paginação e rate limits ainda não foram validados tecnicamente; exige lógica de agendamento, tratamento de erro isolado e logging estruturado.

### Specialized Agent Assignment

**Primary Agents**:
- @dev (cliente HTTP, lógica de sincronização, tratamento de erro)
- @architect (padrão de integração e estratégia de agendamento)

**Supporting Agents**:
- @devops (infraestrutura do job agendado/cron — operação de CI/CD é exclusiva de @devops)
- @data-engineer (revisão do upsert em `sales_entries`)

### Quality Gate Tasks

- [ ] Pre-Commit (@dev): rodar `npm run lint`, `npm run typecheck`, `npm test` e `coderabbit --prompt-only -t uncommitted` antes de marcar a story como completa
- [ ] Pre-PR (@devops): rodar `coderabbit --prompt-only --base main` antes de abrir PR
- [ ] Pre-Deployment (@devops): validar configuração do agendamento e ausência de segredos hardcoded antes de habilitar em produção

### Self-Healing Configuration

**Expected Self-Healing**:
- Primary Agent: @dev (light mode)
- Max Iterations: 2
- Timeout: 15 minutes
- Severity Filter: CRITICAL

**Predicted Behavior**:
- CRITICAL issues: auto_fix (até 2 iterações) — especialmente vazamento de credenciais em logs
- HIGH issues: document_only

### CodeRabbit Focus Areas

**Primary Focus**:
- Tratamento de erro e isolamento de falhas (AC5) — sincronização nunca deve derrubar a aplicação
- Idempotência do upsert em `sales_entries` (AC2)
- Nenhuma credencial (`MOVERE_API_*`) exposta em logs, erros ou commits (NFR4)

**Secondary Focus**:
- Logging estruturado de cada execução (AC3, NFR6)
- Resiliência a rate limits/timeouts desconhecidos da API Moveres

## Tasks / Subtasks

- [x] Task 1: Cliente HTTP para a API Moveres Software (AC: 1)
  - [x] Implementar autenticação na API usando as variáveis de ambiente `MOVERE_API_BASE_URL`, `MOVERE_API_ENVIRONMENT`, `MOVERE_API_USER`, `MOVERE_API_PASSWORD` (nomes definidos em `.env`/`.env.example` — **nunca usar ou expor os valores reais em código, commits ou logs**) [Source: .env.example]
  - [x] Validar o payload recebido com Zod antes de persistir, conforme decisão já registrada no PRD [Source: docs/prd/technical-assumptions.md#Additional Technical Assumptions and Requests]
  - [x] **RISCO CONHECIDO — RESOLVIDO (Achado 8):** cobertura de campos, paginação e rate limits validados tecnicamente contra a API real
- [x] Task 2: Lógica de busca incremental e upsert (AC: 2)
  - [x] Busca por período (`emissaoInicial`/`emissaoFinal`) — a API não suporta filtro por "alterado desde", então cada execução busca o período informado e substitui a janela por completo (TD-04b)
  - [x] Substituição idempotente na tabela `sales_entries` por janela `Data de Emissão + Loja` (`replaceSalesEntriesWindow`, extraído para `shared/sales-entries/` e reusado pela Story TD-07) — não há chave natural de linha (TD-04, Achado 3), então não é upsert por ID
- [ ] Task 3: Agendamento do job (AC: 1 — coordenar com @devops)
  - [ ] **Deliberadamente não implementado nesta story.** Mecanismo (Vercel Cron, GitHub Actions, etc.) depende da decisão de hospedagem (R3), ainda em aberto — delegado a @devops quando essa decisão existir
- [x] Task 4: Execução manual (AC: 4)
  - [x] `npm run sync:moveres -- <emissaoInicial> <emissaoFinal>` (`prisma/sync-moveres.ts`), mesmo padrão CLI de `db:seed`/`db:import` (Constitution Artigo I)
- [x] Task 5: Logging de execuções (AC: 3, NFR6)
  - [x] Log estruturado por loja no console (início/fim implícitos por linha, contagens de notas lidas/ignoradas/linhas apagadas/inseridas, sucesso/erro)
- [x] Task 6: Isolamento de falhas (AC: 5)
  - [x] Cada loja é sincronizada e tratada independentemente (`syncLoja` captura exceções e retorna `{ok:false, error}` sem interromper as demais lojas); falha de login/catálogos (usados por todas as lojas) é a única que aborta a execução inteira, porque nenhuma loja pode ser processada sem eles
- [x] Task 7: Testes e validação
  - [x] Testes unitários com mock de `fetch` (cliente HTTP, schema, mapper, orquestração) — 30 testes novos
  - [x] Testado cenário de falha HTTP (503) isolada a uma loja, sem impedir as demais
  - [x] `npm run lint`, `npm run typecheck` e `npm test` passam (194 testes no total do projeto)
  - [x] Smoke test real contra a API Moveres e o Supabase (`npm run sync:moveres -- 2026-08-03 2026-08-03`): 10/10 lojas, 0 erros
  - [x] File List atualizada

## Dev Notes

**Previous Story Insights**: Depende da Story 1.3 (tabela `sales_entries` deve existir com todos os campos necessários). Revisar o Dev Agent Record da 1.3 para confirmar nomes exatos de campos/tipos antes de mapear o payload da API.

**✅ Spike técnico executado (2026-08-31), bloqueio RETRATADO em 2026-09-02:** ver
`docs/architecture/api-moveres-contract-spike.md` (Achado 8) e TD-03 em
`docs/architecture/tech-decisions.md`. Resumo: autenticação (`POST
/api/LoginComAmbiente`), paginação (`pagina`) e filtro incremental
(`emissaoInicial`/`emissaoFinal`/`codigoLoja`, todos obrigatórios) confirmados contra a
API real. **Itens de linha e parcelas vêm preenchidos** — o spike original checava
`produtos`/`parcelas` (minúsculo, como o Swagger documenta) mas a resposta real usa
`Produtos`/`Parcelas` (maiúsculo), a mesma classe de bug de casing já achada no Achado 3
para `NF`/`nf`. Reteste com a mesma conta (`grupo` continua `"SEM ACESSO"`, sem nenhuma
mudança) confirma 100/100 notas com `Produtos`, 92/100 com `Parcelas`, numa janela de 2
meses. **Task 1 liberada.** Duas dimensões continuam sem mapeamento confirmado —
`familia`/`grupo`/`marca`/`linha` (precisa de lookup via `EstruturasDeItens`, não
mapeado em detalhe) e a condição de pagamento por nota (nenhum campo de `Parcelas` bate
com `codigoCondicao` de `CondicoesDePagamento` na amostra testada) — ficam `null` nesta
primeira versão em vez de um mapeamento adivinhado (Artigo IV — No Invention). O
discriminador Venda/Devolução também não tem amostra real de devolução ainda: a Task 2
sincroniza só notas com `descricaoentradaesaida === "VENDAS DE MERCADORIAS/SERVIÇOS"`
(a assinatura confirmada, 92/100 notas da amostra) — tudo mais (transferências,
garantias, o único caso ambíguo observado) fica de fora por segurança, documentado, não
adivinhado.

**Data Models**: Grava na tabela `sales_entries` definida na Story 1.3 — nenhum novo modelo de dados nesta story, apenas o mapeamento do payload da API para os campos existentes.

**API Specifications**: API REST da Moveres Software, base `https://api.moveresoftware.com`, Swagger disponível em `https://api.moveresoftware.com/swagger/ui/index#/`, documentação em `https://meajuda.moveresoftware.com/a/solutions/articles/27000069342`, autenticação via usuário/senha [Source: docs/brief.md#Technical Considerations; docs/brief.md#Appendices]. O ambiente é identificado pela variável `MOVERE_API_ENVIRONMENT` — assumido como identificador único de tenant ("CirnePneus"), sem múltiplos ambientes a gerenciar (premissa registrada no Project Brief, ainda não validada tecnicamente) [Source: docs/brief.md#Key Assumptions].

**Component Specifications**: N/A (job de backend, sem UI).

**File Locations**: `packages/web/src/features/erp-sync/` seguindo Contract/Service/Repository Pattern do preset [Source: .aiox-core/data/tech-presets/nextjs-react.md#Design Patterns (The Essential 5)].

**Testing Requirements**: Integração com mock da API Moveres, usando MSW (disponível no preset) [Source: .aiox-core/data/tech-presets/nextjs-react.md#Tech Stack; docs/prd/technical-assumptions.md#Testing Requirements: Unit + Integration].

**Technical Constraints**:
- NFR1: dados não podem ter mais de 24h de defasagem — reforça a necessidade de execução diária confiável [Source: docs/prd/requirements.md#Non Functional].
- NFR4: credenciais do ERP nunca em código-fonte versionado — usar exclusivamente as variáveis de ambiente já existentes em `.env` (`MOVERE_API_BASE_URL`, `MOVERE_API_ENVIRONMENT`, `MOVERE_API_USER`, `MOVERE_API_PASSWORD`); nunca imprimir valores reais em logs [Source: docs/prd/requirements.md#Non Functional; .env.example].
- NFR6: falhas devem ser logadas para diagnóstico técnico [Source: docs/prd/requirements.md#Non Functional].
- Mecanismo de agendamento (cron) e hospedagem ainda não definidos — decisão pendente de @architect/@devops [Source: docs/brief.md#Technology Preferences].
- Rate limits da API Moveres desconhecidos — não assumir limites específicos; implementar tratamento de erro genérico e resiliente.
- Nenhum documento de arquitetura (`docs/architecture/`) existe ainda neste projeto.

**Dependencies**: Depende da Story 1.3 (tabela `sales_entries`). A Story 1.5 (Painel) depende desta story — precisa de dados sincronizados para exibir KPIs reais.

### Testing

- **Test file location**: colocation `*.test.ts` dentro de `packages/web/src/features/erp-sync/`
- **Testing framework**: Vitest + MSW para mock da API Moveres
- **Cenários mínimos**: sincronização bem-sucedida (insert + update), falha de autenticação na API, timeout/erro 5xx da API (log registrado, aplicação não derruba), execução manual do job, idempotência (rodar o job duas vezes não duplica registros).

## Change Log

| Date | Version | Description | Author |
|------|---------|--------------|--------|
| 2026-08-27 | 0.1 | Story criada a partir da Epic 1 do PRD | River (SM) |
| 2026-08-27 | 0.2 | Validated GO (9/10) — Status: Draft → Ready | @po |
| 2026-08-27 | 0.2 | Should-fix registrado (não bloqueante): a Task 1 embute uma descoberta técnica de escopo aberto (validar Swagger da API Moveres — cobertura de campos, paginação, rate limits). Se a descoberta revelar divergência relevante em relação ao schema da Story 1.3, PARAR e acionar @po/@architect antes de adaptar o schema por conta própria | @po |
| 2026-08-27 | 0.2 | Condição de entrada registrada: mecanismo de agendamento e hospedagem indefinidos (AC1). AC2–AC5 são plenamente implementáveis sem essa decisão. Recomendação @po: implementar o gatilho manual (AC4) primeiro e tratar o agendamento (AC1) como último passo, delegado a @devops | @po |
| 2026-08-27 | 0.2 | **Referência técnica: `docs/architecture/tech-decisions.md` (TD-03, TD-04).** **⚠️ RISCO ABERTO (TD-03):** o @architect NÃO conseguiu validar o contrato da API Moveres — o Swagger em `api.moveresoftware.com/swagger/ui/index` serve apenas o shell JS (sem endpoints/schemas) e `/swagger/docs/v1` e `/swagger/v1/swagger.json` retornam **404**. Credenciais `MOVERE_API_*` deliberadamente NÃO utilizadas pelo @architect. Estabelecido pela doc pública: auth por **`Authorization: Bearer <token>`**, usuário precisa da rotina **[R1000 — Centro de Integração]**, environment fornecido pelo suporte; a API é segmentada em **grupos** (existem *API de Vendas*, *API Notas Fiscais Emitidas*, *API Notas Emitidas por Estrutura de Itens*); APIs riscadas no Swagger estão obsoletas. **Permanecem NÃO validados:** cobertura de campos, consulta incremental, paginação e rate limits. **Recomendação: executar um spike técnico com credenciais reais ANTES desta story**, entregando o mapa campo-a-campo API → `sales_entries`. **TD-04b — idempotência (AC2/cenário de teste "rodar duas vezes não duplica"):** não há chave natural de linha no export do ERP; verificar no spike se a API expõe ID de linha (preferencial); se não, usar **sincronização por janela (delete-and-replace por `Data de Emissão`+`Loja`)**, que é idempotente sem chave natural e seguro por a app ser somente-leitura. **NÃO** fazer upsert por `Id Lançamento` isolado. Mitigação estrutural: isolar o cliente HTTP atrás de um anti-corruption layer com contrato Zod próprio. Atenção: devoluções chegam com valor **negativo** — validação Zod não pode usar `.positive()` | Aria (@architect) |
| 2026-08-31 | 0.3 | Spike técnico da API Moveres executado (recomendação do TD-03) — ver `docs/architecture/api-moveres-contract-spike.md`. Autenticação/paginação/incremento confirmados; BLOQUEIO encontrado: itens de linha e parcelas retornam vazios (grupo de permissão "SEM ACESSO" na conta configurada). Story permanece **Ready**, não iniciada — ação humana necessária antes da Task 1 | @dev |
| 2026-09-02 | 0.4 | **Bloqueio RETRATADO — Achado 8 do spike.** Não era permissão: bug de casing no spike original (`produtos`/`parcelas` minúsculos verificados, resposta real usa `Produtos`/`Parcelas`). Reteste confirma dado real disponível (100% das notas com itens, 92% com parcelas). Status: Ready → InProgress. Decisão de escopo desta sessão: implementar AC2–AC5 (sincronização manual completa) primeiro, deixando o agendamento automático (AC1) para @devops decidir o mecanismo depois que a hospedagem (R3) for definida — mesma recomendação já registrada pelo @po em 2026-08-27. Duas dimensões ficam `null` nesta primeira versão por falta de mapeamento confirmado (não é invenção): classificação do item (família/grupo/marca/linha) e condição de pagamento por nota — ambas já eram pendências documentadas no Achado 8/6 | @dev |
| 2026-09-02 | 0.5 | Development complete — Status: InProgress → InReview. AC2–AC5 implementados e testados (30 testes novos, 194 no total do projeto). Extraída `replaceSalesEntriesWindow` para `shared/sales-entries/` (reuso com TD-07), agora com escopo de loja de verdade — corrige um gap real da importação de planilha original, que só escopava por data. **Smoke test real contra a API Moveres e o Supabase** (`npm run sync:moveres -- 2026-08-03 2026-08-03`): 10/10 lojas, 0 erros, 550 linhas gravadas. **Achado do smoke test, decisão pendente do usuário antes de um backfill amplo:** linhas sincronizadas via ERP não têm família/grupo/marca/linha nem condição de pagamento (pendências do Achado 8) — diferente da planilha (TD-07), que tem essas dimensões completas. Um backfill histórico amplo via ERP degradaria os relatórios 2.2/2.3/3.2 para o período resincronizado. AC1 (agendamento automático) deliberadamente não implementado — depende de decisão de hospedagem (R3), fora do escopo de @dev | @dev |
| 2026-09-02 | 1.0 | Usuário decidiu: manter a planilha como fonte primária por ora ("Só planilha por enquanto") — sincronização com o ERP fica pronta e testada, mas parada. Dados de 2026-08-03 (afetados pelo smoke test) revertidos para a planilha antes desta revisão. QA Gate CONCERNS — Status: InReview → Done. Reexecutadas e confirmadas todas as validações (lint, typecheck, 194 testes, build). Achados: REQ-001 (medium, AC1 não cumprida por decisão de escopo pré-existente do @po), DATA-001 (medium, dimensões de classificação/condição de pagamento ausentes no ERP — já documentado pelo @dev antes da revisão), DATA-002/TEST-001/PROC-001 (low). Gate: `docs/qa/gates/1.4-job-de-sincronizacao-diaria-com-a-api-moveres-software.yml` | @qa |

## Dev Agent Record

### Agent Model Used

claude-sonnet-5 (@dev / Dex).

### Debug Log References

Reteste ao vivo da API Moveres (Achado 8) documentado em
`docs/architecture/api-moveres-contract-spike.md`. Smoke test real do job
completo: `npm run sync:moveres -- 2026-08-03 2026-08-03` — 10/10 lojas
sincronizadas com sucesso, 0 erros, resultado por loja logado no console (ver
Completion Notes).

### Completion Notes List

- **AC1 (job agendado):** parcialmente atendido de propósito. O gatilho manual
  (AC4) foi implementado por completo; o *agendamento* automático (cron) fica
  para @devops decidir o mecanismo depois que a hospedagem (R3) for definida
  — mesma recomendação já registrada pelo @po em 2026-08-27 (Change Log
  0.2), reafirmada aqui.
- **AC2 (busca + grava/atualiza):** implementado via `replaceSalesEntriesWindow`
  (extraído para `shared/sales-entries/`, reusado também pela importação de
  planilha — TD-07). Substituição idempotente por janela (Data de Emissão +
  Loja), TD-04b, com o filtro de loja agora aplicado de verdade (antes só a
  importação de planilha usava esse repositório, sem escopo de loja — a
  extração corrigiu isso para os dois usos).
- **AC3 (log de execuções):** log estruturado por loja no console
  (`prisma/sync-moveres.ts`), via callback `onLojaResult`.
- **AC4 (execução manual):** `npm run sync:moveres -- <emissaoInicial> <emissaoFinal>`.
- **AC5 (isolamento de falha):** cada loja é sincronizada independentemente;
  uma falha numa loja não impede as demais (testado em
  `sync-moveres.service.test.ts`).
- **Smoke test real (2026-09-02):** `npm run sync:moveres -- 2026-08-03 2026-08-03`
  contra a API e o Supabase reais — 10 lojas, 0 erros, 550 linhas de venda
  gravadas no total do dia.
- **⚠️ Trade-off encontrado no smoke test, decisão pendente do usuário:** as
  linhas sincronizadas via ERP não têm `familia`/`grupo`/`marca`/`linha`
  nem `condicaoPagamento` preenchidos (pendências do Achado 8, sem
  mapeamento confirmado ainda) — diferente das linhas vindas da planilha
  (TD-07), que têm essas dimensões completas. Um backfill histórico completo
  via ERP sync **degradaria** os relatórios que dependem dessas dimensões
  (2.2, 2.3, 3.2) para o período resincronizado. Recomendação: **não** rodar
  um backfill amplo até (a) resolver as pendências de classificação/condição
  de pagamento, ou (b) o usuário decidir conscientemente aceitar o trade-off
  para dados mais recentes que a planilha ainda não cobre.
- Duas dimensões ficam `null` por decisão documentada, não por bug:
  `familia`/`grupo`/`marca`/`linha` e `condicaoPagamento` (Achado 8,
  pendências 1 e 2). "Devolução" não é classificada via ERP ainda — nenhuma
  amostra real observada (Achado 8) para confirmar a assinatura.

### File List

- `packages/web/src/shared/sales-entries/sales-entry.contract.ts` (novo — tipo `SalesEntryRow` extraído de `sales-import`)
- `packages/web/src/shared/sales-entries/replace-window.repository.ts` (novo — extraído de `sales-import`, com escopo de loja opcional)
- `packages/web/src/shared/sales-entries/replace-window.repository.test.ts` (novo)
- `packages/web/src/features/sales-import/sales-import.contract.ts` (modificado — `ParsedSalesEntryRow` agora reexporta `SalesEntryRow`)
- `packages/web/src/features/sales-import/services/import-sales-entries.service.ts` (modificado — usa `replaceSalesEntriesWindow`)
- `packages/web/src/features/sales-import/services/import-sales-entries.service.test.ts` (modificado — tipos)
- `packages/web/src/features/sales-import/index.ts` (modificado — barrel ajustado)
- `packages/web/src/features/erp-sync/erp-sync.contract.ts` (novo)
- `packages/web/src/features/erp-sync/services/movere-client.ts` (novo)
- `packages/web/src/features/erp-sync/services/movere-client.test.ts` (novo)
- `packages/web/src/features/erp-sync/services/nota-fiscal.schema.ts` (novo)
- `packages/web/src/features/erp-sync/services/nota-fiscal.schema.test.ts` (novo)
- `packages/web/src/features/erp-sync/services/map-nota-to-sales-entries.ts` (novo)
- `packages/web/src/features/erp-sync/services/map-nota-to-sales-entries.test.ts` (novo)
- `packages/web/src/features/erp-sync/services/sync-moveres.service.ts` (novo)
- `packages/web/src/features/erp-sync/services/sync-moveres.service.test.ts` (novo)
- `packages/web/src/features/erp-sync/index.ts` (novo)
- `prisma/sync-moveres.ts` (novo — CLI)
- `package.json` (modificado — script `sync:moveres`)
- `tsconfig.seed.json` (modificado — include de `prisma/sync-moveres.ts`)

## QA Results

### Review Date: 2026-09-02

### Reviewed By: Quinn (Test Architect)

### Reviewed Revision: commit:d819682

Auto-escalada para revisão profunda: story de integração externa (API Moveres) com
escrita em `sales_entries`, mesma tabela usada por todos os relatórios da Epic 2/3.

Reexecutei lint, typecheck, testes e build de forma independente — todos passam (194
testes, 32 arquivos). Revisei o smoke test real documentado pelo @dev
(`npm run sync:moveres -- 2026-08-03 2026-08-03`, 10 lojas, 0 erros) e confirmei que a
reversão para a planilha (decisão do usuário) deixou o banco consistente novamente
(23.724 linhas, 0 sem `familia`, faturamento batendo com TD-04).

**Achado de maior peso já era conhecido e documentado pelo próprio @dev antes da
revisão** (DATA-001: dimensões de classificação/condição de pagamento ausentes no ERP)
— não é uma surpresa encontrada agora, é uma limitação real da fonte de dados, corretamente
não-inventada. AC1 não cumprida é uma decisão de escopo já aprovada pelo @po em
2026-08-27, não uma falta do @dev.

### Gate Status

Gate: CONCERNS → docs/qa/gates/1.4-job-de-sincronizacao-diaria-com-a-api-moveres-software.yml

### Lifecycle Transition

CONCERNS: InReview → Done (aplicada no Status e no Change Log desta story).
