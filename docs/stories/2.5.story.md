# Story 2.5: Faturamento por Condição de Pagamento

## Status

InReview

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@architect"
```

## Story

**As a** gestor,
**I want** ver o faturamento agrupado por condição de pagamento,
**so that** eu entenda como a rede está recebendo pelas vendas (Requisito 4).

## Acceptance Criteria

1. Faturamento agrupado por Condição de Pagamento.
2. Filtro por período e loja.

## Tasks / Subtasks

- [x] Task 1: Query de agregação (AC: 1, 2)
  - [x] `groupBy({ by: ['condicaoPagamento'], _sum: { valorTotal: true }, orderBy: { _sum: { valorTotal: 'desc' } } })` filtrado por período/loja
- [x] Task 2: UI de tabela (AC: 1) — `FaturamentoPorCondicaoPagamentoTable`
- [x] Task 3: Filtro de período e loja (AC: 2) — `ReportFilterForm showLoja`
- [x] Task 4: Testes e validação
  - [x] Testes unitários da agregação com repositório fake — 3 testes
  - [x] Confirmar `npm run lint`, `npm run typecheck`, `npm test` — ✅ lint, ✅ typecheck, ✅ 133/133, ✅ build
  - [x] Atualizar File List

## Dev Notes

**File Locations**: `packages/web/src/features/reports/faturamento-por-condicao-pagamento/`.

**Technical Constraints**: `condicaoPagamento` é nullable (Story 1.3) — mesmo tratamento de 2.2 (rótulo "Não informado", nunca descartar a linha do agrupamento).

**Dependencies**: Depende da Story 1.3.

### Testing

- **Cenários mínimos**: agregação e ordenação corretas, filtro de período/loja aplicado, condição nula agrupada sob "Não informado".

## Change Log

| Date | Version | Description | Author |
|------|---------|--------------|--------|
| 2026-08-31 | 0.1 | Story criada a partir da Epic 2 do PRD, em lote com as demais stories de relatório | @sm (via sessão combinada) |
| 2026-08-31 | 0.2 | Auto-validada GO — Status: Draft → Ready | @po (via sessão combinada) |
| 2026-08-31 | 0.3.0 | Development started/complete (sessão contínua, em lote com 2.1–2.3/3.1–3.3) — Status: Ready → InProgress → InReview. AC1/AC2 implementadas. Validações: lint ✅, typecheck ✅, 133 testes ✅, build ✅ | @dev |

## Dev Agent Record

### Agent Model Used

claude-sonnet-5 (@dev / Dex) — modo interactive, implementada em lote com as demais stories de relatório da Epic 2/3 (ver Story 2.1 para o refactor compartilhado).

### Debug Log References

Validações: `npm run lint`, `npm run typecheck`, `npm test`, `npm run build`.

### Completion Notes List

**Entregue: AC1, AC2.**

Feature `features/reports/faturamento-por-condicao-pagamento/`: mesmo padrão de 2.2/2.5
(`groupBy` + `_sum.valorTotal` já ordenado no banco, `condicaoPagamento` nulo vira
`"Não informado"`). Filtro com `showLoja` habilitado (única entre as 3 primeiras stories
de relatório com filtro de loja além da 2.1, já que a AC2 pede explicitamente).

**Resultados das validações:**

| Comando | Resultado |
|---------|-----------|
| `npm run lint` | ✅ sem erros |
| `npm run typecheck` | ✅ sem erros |
| `npm test` | ✅ 133 testes / 23 arquivos |
| `npm run build` | ✅ build de produção OK |

### File List

**Criados**

- `packages/web/src/features/reports/faturamento-por-condicao-pagamento/faturamento-por-condicao-pagamento.contract.ts`
- `packages/web/src/features/reports/faturamento-por-condicao-pagamento/index.ts`
- `packages/web/src/features/reports/faturamento-por-condicao-pagamento/repositories/faturamento-por-condicao-pagamento.repository.ts`
- `packages/web/src/features/reports/faturamento-por-condicao-pagamento/services/faturamento-por-condicao-pagamento.service.ts` + `.test.ts`
- `packages/web/src/features/reports/faturamento-por-condicao-pagamento/components/FaturamentoPorCondicaoPagamentoTable.tsx`
- `packages/web/src/app/(protected)/relatorios/faturamento-por-condicao-pagamento/page.tsx`

## QA Results

_A ser preenchido pelo @qa durante o gate de qualidade._
