# Story 2.5: Faturamento por Condição de Pagamento

## Status

Ready

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@architect"
```

## Story

**As a** gestor,
**I want** ver o faturamento agrupado por condição de pagamento,
**so that** eu entenda como a rede está recebendo pelas vendas (Requisito 4).

## Acceptance Criteria

1. Faturamento agrupado por Condição de Pagamento.
2. Filtro por período e loja.

## Tasks / Subtasks

- [ ] Task 1: Query de agregação (AC: 1, 2)
  - [ ] `groupBy({ by: ['condicaoPagamento'], _sum: { valorTotal: true }, orderBy: { _sum: { valorTotal: 'desc' } } })` filtrado por período/loja
- [ ] Task 2: UI de tabela (AC: 1)
- [ ] Task 3: Filtro de período e loja (AC: 2) — reutilizar componente compartilhado
- [ ] Task 4: Testes e validação
  - [ ] Testes unitários da agregação com repositório fake
  - [ ] Confirmar `npm run lint`, `npm run typecheck`, `npm test`
  - [ ] Atualizar File List

## Dev Notes

**File Locations**: `packages/web/src/features/reports/faturamento-por-condicao-pagamento/`.

**Technical Constraints**: `condicaoPagamento` é nullable (Story 1.3) — mesmo tratamento de 2.2 (rótulo "Não informado", nunca descartar a linha do agrupamento).

**Dependencies**: Depende da Story 1.3.

### Testing

- **Cenários mínimos**: agregação e ordenação corretas, filtro de período/loja aplicado, condição nula agrupada sob "Não informado".

## Change Log

| Date | Version | Description | Author |
|------|---------|--------------|--------|
| 2026-08-31 | 0.1 | Story criada a partir da Epic 2 do PRD, em lote com as demais stories de relatório | @sm (via sessão combinada) |
| 2026-08-31 | 0.2 | Auto-validada GO — Status: Draft → Ready | @po (via sessão combinada) |

## Dev Agent Record

### Agent Model Used

_A ser preenchido pelo @dev durante a implementação._

### Debug Log References

_A ser preenchido pelo @dev durante a implementação._

### Completion Notes List

_A ser preenchido pelo @dev durante a implementação._

### File List

_A ser preenchido pelo @dev durante a implementação._

## QA Results

_A ser preenchido pelo @qa durante o gate de qualidade._
