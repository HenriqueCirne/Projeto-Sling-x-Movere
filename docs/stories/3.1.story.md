# Story 3.1: Ranking de Clientes por Faturamento

## Status

InReview

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@architect"
```

## Story

**As a** gestor,
**I want** ver um ranking de clientes por faturamento,
**so that** eu identifique meus clientes mais valiosos (Requisito 5).

## Acceptance Criteria

1. Lista de clientes ordenada por faturamento total, decrescente.
2. Filtro por período.
3. Exibe posição/rank e valor faturado por cliente.

## Tasks / Subtasks

- [x] Task 1: Query de ranking (AC: 1, 3)
  - [x] `groupBy({ by: ['cliente'], _sum: { valorTotal: true }, orderBy: { _sum: { valorTotal: 'desc' } } })`, excluindo `cliente: null` (no próprio `where`, não filtrado depois)
  - [x] Posição/rank é o índice do array ordenado (+1) — calculado na camada de serviço, não no banco
- [x] Task 2: UI da lista/tabela (AC: 1, 3) — `RankingClientesTable`
- [x] Task 3: Filtro de período (AC: 2) — reutilizar componente compartilhado
- [x] Task 4: Testes e validação
  - [x] Testes unitários do ranking (ordenação + numeração de posição) com repositório fake — 3 testes
  - [x] Confirmar `npm run lint`, `npm run typecheck`, `npm test` — ✅ lint, ✅ typecheck, ✅ 133/133, ✅ build
  - [x] Atualizar File List

## Dev Notes

**File Locations**: `packages/web/src/features/reports/ranking-clientes/`.

**Technical Constraints**: `cliente` nulo é excluído do ranking (não faz sentido rankear "cliente não informado") — diferente de 2.2/2.5, que agrupam nulos sob rótulo explícito. A diferença é deliberada: aqui o nulo não é uma dimensão do relatório, é ausência de dado que invalidaria a posição no ranking.

**Dependencies**: Depende da Story 1.3.

### Testing

- **Cenários mínimos**: ordenação decrescente por faturamento, posição 1-based correta, cliente nulo excluído do ranking (não aparece como linha).

## Change Log

| Date | Version | Description | Author |
|------|---------|--------------|--------|
| 2026-08-31 | 0.1 | Story criada a partir da Epic 3 do PRD, em lote com as demais stories de relatório | @sm (via sessão combinada) |
| 2026-08-31 | 0.2 | Auto-validada GO — Status: Draft → Ready | @po (via sessão combinada) |
| 2026-08-31 | 0.3.0 | Development started/complete (sessão contínua, em lote com 2.1–2.3/2.5/3.2/3.3) — Status: Ready → InProgress → InReview. AC1–AC3 implementadas. Validações: lint ✅, typecheck ✅, 133 testes ✅, build ✅ | @dev |

## Dev Agent Record

### Agent Model Used

claude-sonnet-5 (@dev / Dex) — modo interactive, implementada em lote com as demais stories de relatório da Epic 2/3 (ver Story 2.1 para o refactor compartilhado).

### Debug Log References

Validações: `npm run lint`, `npm run typecheck`, `npm test`, `npm run build`.

### Completion Notes List

**Entregue: AC1, AC2, AC3.**

Feature `features/reports/ranking-clientes/`: `groupBy(['cliente'])` com `cliente: { not: null
}` já no `where` (não um filtro depois — cliente nulo nunca entra no agrupamento, diferente de
2.2/2.5, que agrupam nulos sob "Não informado"). A diferença é deliberada, documentada no
contrato: ranking de pessoa não tem leitura sensata para "Não informado" na posição 1, 2, 3...
Posição é o índice do array (+1) do próprio serviço, nunca uma segunda ordenação — não pode
divergir da ordem real devolvida pelo banco.

**Resultados das validações:**

| Comando | Resultado |
|---------|-----------|
| `npm run lint` | ✅ sem erros |
| `npm run typecheck` | ✅ sem erros |
| `npm test` | ✅ 133 testes / 23 arquivos |
| `npm run build` | ✅ build de produção OK |

### File List

**Criados**

- `packages/web/src/features/reports/ranking-clientes/ranking-clientes.contract.ts`
- `packages/web/src/features/reports/ranking-clientes/index.ts`
- `packages/web/src/features/reports/ranking-clientes/repositories/ranking-clientes.repository.ts`
- `packages/web/src/features/reports/ranking-clientes/services/ranking-clientes.service.ts` + `.test.ts`
- `packages/web/src/features/reports/ranking-clientes/components/RankingClientesTable.tsx`
- `packages/web/src/app/(protected)/relatorios/ranking-clientes/page.tsx`

## QA Results

_A ser preenchido pelo @qa durante o gate de qualidade._
