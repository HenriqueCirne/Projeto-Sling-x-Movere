# Story 3.3: Desempenho por Atendente

## Status

Done

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@architect"
```

## Story

**As a** gestor,
**I want** ver quantidade e faturamento por atendente,
**so that** eu avalie a equipe comercial (Requisito 8).

## Acceptance Criteria

1. Relatório exibe quantidade e faturamento por atendente.
2. O melhor desempenho do período é destacado visualmente.
3. Filtro por período e loja.

## Tasks / Subtasks

- [x] Task 1: Query de agregação (AC: 1)
  - [x] `groupBy({ by: ['atendente'], _sum: { valorTotal: true, quantidade: true }, orderBy: { _sum: { valorTotal: 'desc' } } })`
- [x] Task 2: UI com destaque do melhor desempenho (AC: 2)
  - [x] "Melhor desempenho" = maior faturamento (`_sum.valorTotal`) no período — mesmo critério usado para ordenar; o primeiro da lista já ordenada é o destaque (`melhorDesempenho: index === 0`), sem lógica duplicada. Destaque visual (badge + fundo) em `DesempenhoAtendenteTable`
- [x] Task 3: Filtro de período e loja (AC: 3) — `ReportFilterForm showLoja`
- [x] Task 4: Testes e validação
  - [x] Testes unitários da agregação e da identificação do melhor desempenho — 3 testes
  - [x] Confirmar `npm run lint`, `npm run typecheck`, `npm test` — ✅ lint, ✅ typecheck, ✅ 133/133, ✅ build
  - [x] Atualizar File List

## Dev Notes

**File Locations**: `packages/web/src/features/reports/desempenho-atendente/`.

**Technical Constraints**: `atendente` nullable (Story 1.3) — mesmo tratamento de agrupamento sob "Não informado" das demais dimensões descritivas (não é ranking de pessoa, então não faz sentido excluir como em 3.1).

**Dependencies**: Depende da Story 1.3.

### Testing

- **Cenários mínimos**: agregação de quantidade e faturamento por atendente, melhor desempenho corretamente identificado como o de maior faturamento, atendente nulo agrupado sob "Não informado".

## Change Log

| Date | Version | Description | Author |
|------|---------|--------------|--------|
| 2026-08-31 | 0.1 | Story criada a partir da Epic 3 do PRD, em lote com as demais stories de relatório | @sm (via sessão combinada) |
| 2026-08-31 | 0.2 | Auto-validada GO — Status: Draft → Ready | @po (via sessão combinada) |
| 2026-08-31 | 0.3.0 | Development started/complete (sessão contínua, em lote com 2.1–2.3/2.5/3.1/3.2 — última story implementada) — Status: Ready → InProgress → InReview. AC1–AC3 implementadas. Validações: lint ✅, typecheck ✅, 133 testes ✅, build ✅ | @dev |
| 2026-09-01 | 0.4.0 | QA Gate CONCERNS — Status: InReview → Done. Achado: TEST-001 (medium — limitação de ambiente recorrente). Última story da leva revisada; 7/9 stories da Epic 2/3 em Done, 2.4 e 3.4 seguem bloqueadas. Gate: `docs/qa/gates/3.3-desempenho-por-atendente.yml` | @qa |

## Dev Agent Record

### Agent Model Used

claude-sonnet-5 (@dev / Dex) — modo interactive, última story implementada nesta sessão em lote da Epic 2/3 (ver Story 2.1 para o refactor compartilhado).

### Debug Log References

Validações: `npm run lint`, `npm run typecheck`, `npm test`, `npm run build`, e a rodada completa de validação manual via dev server cobrindo as 7 rotas novas de relatório (todas redirecionam corretamente sem sessão, com `callbackUrl` preservado, após o fix do proxy — ver Story 2.1).

### Completion Notes List

**Entregue: AC1, AC2, AC3.**

Feature `features/reports/desempenho-atendente/`: `groupBy(['atendente'])` com `_sum` de
`valorTotal` e `quantidade`, ordenado por faturamento. "Melhor desempenho" (AC2) é
`index === 0` do array já ordenado — nunca uma segunda comparação que poderia divergir do
critério de ordenação. Destaque visual (badge + fundo âmbar) na tabela.

**Sessão da Epic 2/3 concluída nesta story:** 7 de 9 stories implementadas (2.1, 2.2, 2.3,
2.5, 3.1, 3.2, 3.3); 2.4 e 3.4 permanecem bloqueadas por decisão de negócio/dado real (ver
os próprios arquivos delas). 133 testes no total, todos os gates verdes. Próximo passo:
QA gate.

**Resultados das validações:**

| Comando | Resultado |
|---------|-----------|
| `npm run lint` | ✅ sem erros |
| `npm run typecheck` | ✅ sem erros |
| `npm test` | ✅ 133 testes / 23 arquivos |
| `npm run build` | ✅ build de produção OK — 7 rotas de relatório presentes |
| Validação manual (dev server) | ✅ todas as 7 rotas redirecionam corretamente sem sessão |

### File List

**Criados**

- `packages/web/src/features/reports/desempenho-atendente/desempenho-atendente.contract.ts`
- `packages/web/src/features/reports/desempenho-atendente/index.ts`
- `packages/web/src/features/reports/desempenho-atendente/repositories/desempenho-atendente.repository.ts`
- `packages/web/src/features/reports/desempenho-atendente/services/desempenho-atendente.service.ts` + `.test.ts`
- `packages/web/src/features/reports/desempenho-atendente/components/DesempenhoAtendenteTable.tsx`
- `packages/web/src/app/(protected)/relatorios/desempenho-atendente/page.tsx`

## QA Results

### Review Date: 2026-09-01

### Reviewed By: Quinn (Test Architect)

### Reviewed Revision: commit:5b2630a

Revisão final desta leva de 7 stories (2.1, 2.2, 2.3, 2.5, 3.1, 3.2 e esta). Reexecutei
lint/typecheck/test/build e validei manualmente as 7 rotas novas de relatório.

### Code Quality Assessment

Consistente com o padrão estabelecido nas 6 stories anteriores desta leva:
Contract/Service/Repository sobre `sales_entries`, `groupBy` type-safe (sem SQL cru),
filtro de período+loja compartilhado. "Melhor desempenho" corretamente derivado do
`index === 0` da lista já ordenada, sem lógica duplicada que pudesse divergir.

### Compliance Check

- Testing Strategy: ✓ com ressalva — TEST-001 (limitação de ambiente recorrente)
- All ACs Met: ✓ — AC1–AC3

### Security Review

Nenhum problema em nenhuma das 7 stories desta leva. Dois achados de infraestrutura reais
foram corrigidos durante a implementação (não achados de QA, já fechados pelo @dev antes
da revisão): proxy não cobria `/relatorios` (MNT-002 do gate 1.5, materializado e
corrigido) e 2 Client Components importando do barrel server-only (corrigido).

### Gate Status

Gate: CONCERNS → docs/qa/gates/3.3-desempenho-por-atendente.yml

### Lifecycle Transition

CONCERNS: InReview → Done (aplicada no Status e no Change Log desta story).
