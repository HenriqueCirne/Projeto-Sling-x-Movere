# Story 3.2: Tipo de Preço × Loja × Item

## Status

InReview

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@architect"
```

## Story

**As a** gestor,
**I want** cruzar Tipo de Preço, Loja e Item,
**so that** eu analise a política de preços praticada (Requisito 7).

## Acceptance Criteria

1. Relatório cruza Tipo de Preço, Loja e Item.
2. Seletor permite escolher a dimensão de foco, replicando o comportamento da planilha atual.

## Tasks / Subtasks

- [x] Task 1: Query de cruzamento (AC: 1)
  - [x] `groupBy({ by: ['tipoPreco', 'loja', 'item'], _sum: { valorTotal: true, quantidade: true } })` — uma única query com as 3 dimensões; o "seletor de foco" (AC2) é sobre COMO exibir/pivotar o mesmo resultado, não uma query diferente por dimensão
- [x] Task 2: UI com seletor de dimensão de foco (AC: 2)
  - [x] Client-side: `sortByFoco()` (pura, testada) reordena o mesmo array de resultados conforme a dimensão escolhida (Tipo de Preço, Loja ou Item) — sem novo round-trip ao trocar o seletor. Interpretação registrada nos Dev Notes: reordenação, não re-agregação — nenhum documento especifica o pivot exato da planilha
- [x] Task 3: Testes e validação
  - [x] Testes unitários da query de cruzamento e do pivot client-side — 6 testes (2 do serviço + 4 do `sortByFoco`)
  - [x] Confirmar `npm run lint`, `npm run typecheck`, `npm test` — ✅ lint, ✅ typecheck, ✅ 133/133, ✅ build
  - [x] Atualizar File List

## Dev Notes

**File Locations**: `packages/web/src/features/reports/tipo-preco-loja-item/`.

**Component Specifications**: "Seletor permite escolher a dimensão de foco, replicando o comportamento da planilha atual" — nenhum documento do projeto especifica exatamente como a planilha pivota; a interpretação mínima e não-inventada é: mesmos 3 dados, dimensão primária de agrupamento escolhível pelo usuário na UI.

**Dependencies**: Depende da Story 1.3.

### Testing

- **Cenários mínimos**: query retorna as 3 dimensões corretamente agregadas; trocar o seletor de foco reagrupa os mesmos dados sem nova chamada ao servidor.

## Change Log

| Date | Version | Description | Author |
|------|---------|--------------|--------|
| 2026-08-31 | 0.1 | Story criada a partir da Epic 3 do PRD, em lote com as demais stories de relatório | @sm (via sessão combinada) |
| 2026-08-31 | 0.2 | Auto-validada GO — Status: Draft → Ready | @po (via sessão combinada) |
| 2026-08-31 | 0.3.0 | Development started/complete (sessão contínua, em lote com 2.1–2.3/2.5/3.1/3.3) — Status: Ready → InProgress → InReview. AC1/AC2 implementadas com interpretação registrada (pivot = reordenação, não re-agregação). Validações: lint ✅, typecheck ✅, 133 testes ✅, build ✅ | @dev |

## Dev Agent Record

### Agent Model Used

claude-sonnet-5 (@dev / Dex) — modo interactive, implementada em lote com as demais stories de relatório da Epic 2/3 (ver Story 2.1 para o refactor compartilhado).

### Debug Log References

Validações: `npm run lint`, `npm run typecheck`, `npm test`, `npm run build`.

### Completion Notes List

**Entregue: AC1, AC2 (com interpretação registrada).**

Feature `features/reports/tipo-preco-loja-item/`: uma única query `groupBy` com as 3
dimensões (AC1). O "seletor de dimensão de foco" (AC2) foi implementado como reordenação
client-side (`sortByFoco`, pura) do mesmo array já buscado — coloca a dimensão escolhida como
chave primária de agrupamento visual, com faturamento decrescente como critério de desempate.
**Interpretação, não invenção:** nenhum documento do projeto especifica exatamente como a
planilha original pivota esse cruzamento; a leitura mínima "mesmos 3 dados, ordem de exibição
escolhível" atende a AC literalmente sem inventar uma re-agregação não pedida.

**Cuidado de bundle (mesmo aplicado em 2.1):** `TipoPrecoLojaItemTable` é Client Component
(precisa de `useState` para o seletor) — importa `formatCurrency` do módulo concreto
`@/features/dashboard/format`, não do barrel `@/features/dashboard`, que também reexporta
código que arrasta Prisma.

**Resultados das validações:**

| Comando | Resultado |
|---------|-----------|
| `npm run lint` | ✅ sem erros |
| `npm run typecheck` | ✅ sem erros |
| `npm test` | ✅ 133 testes / 23 arquivos |
| `npm run build` | ✅ build de produção OK |

### File List

**Criados**

- `packages/web/src/features/reports/tipo-preco-loja-item/tipo-preco-loja-item.contract.ts`
- `packages/web/src/features/reports/tipo-preco-loja-item/index.ts`
- `packages/web/src/features/reports/tipo-preco-loja-item/sort-by-foco.ts` + `.test.ts`
- `packages/web/src/features/reports/tipo-preco-loja-item/repositories/tipo-preco-loja-item.repository.ts`
- `packages/web/src/features/reports/tipo-preco-loja-item/services/tipo-preco-loja-item.service.ts` + `.test.ts`
- `packages/web/src/features/reports/tipo-preco-loja-item/components/TipoPrecoLojaItemTable.tsx`
- `packages/web/src/app/(protected)/relatorios/tipo-preco-loja-item/page.tsx`

## QA Results

_A ser preenchido pelo @qa durante o gate de qualidade._
