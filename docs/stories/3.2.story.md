# Story 3.2: Tipo de Preço × Loja × Item

## Status

Ready

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@architect"
```

## Story

**As a** gestor,
**I want** cruzar Tipo de Preço, Loja e Item,
**so that** eu analise a política de preços praticada (Requisito 7).

## Acceptance Criteria

1. Relatório cruza Tipo de Preço, Loja e Item.
2. Seletor permite escolher a dimensão de foco, replicando o comportamento da planilha atual.

## Tasks / Subtasks

- [ ] Task 1: Query de cruzamento (AC: 1)
  - [ ] `groupBy({ by: ['tipoPreco', 'loja', 'item'], _sum: { valorTotal: true, quantidade: true } })` — uma única query com as 3 dimensões; o "seletor de foco" (AC2) é sobre COMO exibir/pivotar o mesmo resultado, não uma query diferente por dimensão
- [ ] Task 2: UI com seletor de dimensão de foco (AC: 2)
  - [ ] Client-side: o mesmo array de resultados é reagrupado/pivotado no navegador conforme a dimensão escolhida (Tipo de Preço, Loja ou Item) — sem novo round-trip ao trocar o seletor, mesmo padrão de "sem round-trip extra" da 2.1
- [ ] Task 3: Testes e validação
  - [ ] Testes unitários da query de cruzamento e do pivot client-side
  - [ ] Confirmar `npm run lint`, `npm run typecheck`, `npm test`
  - [ ] Atualizar File List

## Dev Notes

**File Locations**: `packages/web/src/features/reports/tipo-preco-loja-item/`.

**Component Specifications**: "Seletor permite escolher a dimensão de foco, replicando o comportamento da planilha atual" — nenhum documento do projeto especifica exatamente como a planilha pivota; a interpretação mínima e não-inventada é: mesmos 3 dados, dimensão primária de agrupamento escolhível pelo usuário na UI.

**Dependencies**: Depende da Story 1.3.

### Testing

- **Cenários mínimos**: query retorna as 3 dimensões corretamente agregadas; trocar o seletor de foco reagrupa os mesmos dados sem nova chamada ao servidor.

## Change Log

| Date | Version | Description | Author |
|------|---------|--------------|--------|
| 2026-08-31 | 0.1 | Story criada a partir da Epic 3 do PRD, em lote com as demais stories de relatório | @sm (via sessão combinada) |
| 2026-08-31 | 0.2 | Auto-validada GO — Status: Draft → Ready | @po (via sessão combinada) |

## Dev Agent Record

### Agent Model Used

_A ser preenchido pelo @dev durante a implementação._

### Debug Log References

_A ser preenchido pelo @dev durante a implementação._

### Completion Notes List

_A ser preenchido pelo @dev durante a implementação._

### File List

_A ser preenchido pelo @dev durante a implementação._

## QA Results

_A ser preenchido pelo @qa durante o gate de qualidade._
